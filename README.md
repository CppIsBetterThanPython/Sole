# Sole

Sole is a software rendering engine (CPU side rendering) that I made to learn about the modern graphics pipeline and how to implement it using projection matrices, quaternions, etc.

It is not very optimised or efficient however it does have a relational system for objects, where objects can be the children of other objects.
